import orderedList from "./ordered-list";
import unorderedList from "./unordered-list";
import { orderedListTag, unorderedListTag, listItemTag } from "./list-tag";

export default {
  orderedList,
  unorderedList,
  orderedListTag,
  unorderedListTag,
  listItemTag,
};
