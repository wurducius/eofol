- allow configure src-maps, minify, cors
- extract base/global styles and such and allow import to every view

* finish chunking
* state batching/queue (pipe/reduce state through effects)
* jsx compatibility

??? memo, should component update, memoize handlers and values
??? effect deps
??? attribute changing messaging between custom elements as props
??? collect styles for view and inject inline as style tag instead of external stylesheet link
